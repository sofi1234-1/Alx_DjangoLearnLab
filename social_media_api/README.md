# Social Media API

## Project Setup

1. Clone the repository.
2. Install dependencies with `pip install django djangorestframework`.
3. Navigate to the project directory and run migrations:

   ```bash
   python manage.py migrate